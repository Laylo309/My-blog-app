<%= form_with url: post_comments_path(@post.id), scope: :comment do |form| %>
 <div>
		<%= form.text_area :text, placeholder: "Enter comment here",class: "user-data mt-4 form-control mb-4", rows: 3, required: true %>
	</div>
	<%= form.submit "Publish" %>
<% end %>
